{
  "name": "cloudflare-token-vpn",
  "version": "0.1.0",
  "private": true,
  "description": "Token 制 VPN 服务：中心 API（workerd 自建）+ 售卖站点 + Xray 多节点直连",
  "workspaces": [
    "workers/api",
    "pages"
  ],
  "scripts": {
    "dev:api": "npm run dev --workspace workers/api",
    "dev:pages": "npm run dev --workspace pages",
    "build:pages": "npm run build --workspace pages",
    "deploy:api": "bash scripts/deploy-api-local.sh",
    "deploy:site": "bash scripts/deploy-site-local.sh"
  }
}
